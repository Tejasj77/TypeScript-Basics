const enum Directon {
    Up=1,
    Down,
    Left,
    Right
}
let dir = Directon.Down
console.log(dir)

const enum Iteration {
    pahila,
    dusra=10,
    tisra,
    chotha
}

let iter = Iteration.tisra
console.log(iter)