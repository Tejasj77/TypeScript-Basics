const enum Directon {
    Up=1,
    Down,
    Left,
    Right
}
let dir = Directon.Down
console.log(dir)

const enum Iterator {
    pahila,
    dusra=10,
    tisra,
    chotha
}

let iter = Iterator.tisra
console.log(iter)